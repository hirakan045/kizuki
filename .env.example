# api/report.ts が使う環境変数。実際の値はVercelの環境変数に設定する（このファイルはコミットしてよい）。
ANTHROPIC_API_KEY=
APP_SHARED_SECRET=

# アプリ（src/lib/reportApi.ts）が使う環境変数。実際の値はローカルの .env に設定する。
# EXPO_PUBLIC_ 接頭辞のためクライアントバンドルに埋め込まれる。
EXPO_PUBLIC_API_URL=
EXPO_PUBLIC_APP_SHARED_SECRET=
