# 開発ログ — きづき

作業中に得た知見・詰まった点を記録する。進捗管理は `WBS.md` を参照。

---

## 2026-08-14（続き）

### やったこと

5.2（発見ロジック、D-1〜D-6のアプリ側判定）に着手する予定だったが、利用者の意向により方針転換。**「今日の発見」の判定・選択そのものをAIに任せる**設計に変更した。アプリ側の決定論的ロジック（`src/logic/discovery.ts`を作る予定だった）は不要になり、`api/report.ts`のリクエスト形式を直近30日分の生データ配列に変更、システムプロンプトにD-1〜D-6の判断基準を追加した。レスポンスはtool use（`submit_report`）で`report`と`discoveryKind`を構造化して受け取る形にした。PRD F-3-5もこの方針に合わせて更新し、WBS 5.2.1〜5.2.7は❌にして5.3.1に統合した。

### 詰まったこと・判断

- 当初のPRD案「D-5週次変化はデータ14日以上」は、HealthKitの欠損（Watch未装着日等）を考慮すると実質「毎日記録」に近い厳しい条件になってしまうと利用者から指摘があり、**「前週・今週それぞれ7日中5日以上」に緩和**した。閾値は思いつきで決めず、実データの欠損しやすさを踏まえて決めるべきという教訓。
- D-6（データ蓄積の予告）の文言は「あと3日分たまると〜」のような**データ量の予告は許容だが、「あと◯歩」のような個人の行動目標の提示は禁止**、という区別をプロンプトに明記する必要があった。両者は表面上似ているが、主語が「データの状態」か「これからの行動」かで判定原則（急かさない）への抵触有無が変わる。
- 実装中、ESLintの`no-restricted-syntax`ルールが`target`という変数名を禁止語（`docs/glossary.md`の「目標/goal/target」）として検出した。`targetDay`に変更して解消。**このプロジェクトはただの命名規則ではなく、設計原則をLintで機械的に強制している**ことを再確認した。

### 次回やること

- 5.3.2: アプリからのAPI呼び出し実装（`history`配列・`recentDiscoveryKinds`の組み立て、`discoveryKind`のDayRecordへの保存を含む）。ここで`APP_SHARED_SECRET`をローテーションして実際の疎通確認も行う
- 5.3.3〜5.3.7: 生成トリガー・保存・エラー処理・生成中表示・プロンプト調整（自分のデータで確認）
- 4.3.3: 保留中。`RNSVGSvgView`エラーの解決から再開

### 未解決・保留

- `docs/architecture.md`が依然として空ファイルのまま（前回セッションから継続）

---

## 2026-08-14

### やったこと

4.3.3（グラフ）を保留にし、5.1.3（AIレポートAPIのデプロイ・疎通確認）を先に着手。Vercelプロジェクトを`kizuki`から`kizuki-api`にリネームし、Production環境に`ANTHROPIC_API_KEY`を追加・再デプロイ。認証ヘッダーなしのリクエストで401が返ることを確認した。

### 詰まったこと・判断

- ローカルの`main`がPR #5（AIレポートサーバー、8/10に別セッションでマージ済み）の分だけ古かった。作業前に`git pull`で追従。**ブランチを切る前に必ずmainの最新化を確認する習慣が必要。**
- Vercelプロジェクトが`kizuki`と`kizuki-api`の2つ存在していた。`kizuki-api`は空（404）で、実際にAPIが動いているのは`kizuki`だった。利用者と相談し、空の`kizuki-api`を削除した上で`kizuki`を`kizuki-api`にリネーム（`vercel project rename`）。git連携・環境変数はリネームに影響されないことを確認。
- `vercel deploy --prod`をローカルから実行したところ、CPU・ネットワークともに活動がないまま7分以上応答がなく停止と判断してkillした。**その直後にバックグラウンドタスクの完了通知が来て、実際にはアップロード自体は数秒で終わり"Building…"の表示中だった。**ローカルの`vercel deploy`は不安定に見えたため、代わりに`vercel redeploy <既存デプロイURL> --target production`（クラウド側で既存ソースを再ビルド）を使ったところ47秒で完了した。**この構成ではgit連携の`vercel redeploy`を使う方が安定している可能性が高い。**
- `APP_SHARED_SECRET`が「Sensitive」設定のため、CLIから値を再取得できないことが判明（`vercel env pull`では`[SENSITIVE]`というプレースホルダーしか返らない）。かつクライアント側（アプリ）はまだこのヘッダーを送る実装がなく（5.3.2未着手）、実質的にこの値は失われた状態。**Sensitive変数は書き込み専用と心得ること。** 今回は5.1.3の完了条件を「401が返ること（認証ゲートが機能していること）」までとし、Claude APIまでの実際の疎通確認は5.3.2（アプリ実装でシークレットを組み込むタイミング）に持ち越す判断を利用者と合意した。

### 次回やること

- 4.3.3: 保留中。`RNSVGSvgView`エラーの解決から再開
- 5.2.1, 5.2.2: 発見ロジック（Must分）
- 5.3: アプリからのAPI呼び出し実装時に、`APP_SHARED_SECRET`をローテーションしてアプリ側に組み込み、実際にClaude APIまでの疎通を確認する

### 未解決・保留

- `docs/architecture.md`が依然として空ファイルのまま（前回セッションから継続）

---

## 2026-08-10（続き）

### やったこと

PR #4（画面実装）のマージを確認し、WBSの実績を修正。4.3.3の折れ線グラフ（`TrendChart` + `react-native-svg`）を実装。5.1 AIレポート生成サーバー（Vercel Serverless Function + Claude API）を実装しPR #5を作成。

### 詰まったこと・判断

- **react-native-svg導入後、development buildの再ビルドが必要だった。**再ビルド後も実機で `Unimplemented component: RNSVGSvgView` エラーが発生し、原因未確定のままセッション終了。Metroキャッシュ（`expo start --dev-client --clear`）とアプリの完全再起動を提案したが、解決確認は次回に持ち越し。
- AIレポートのサーバー側は、PRDでは「Vercel/Cloud Run」と仮置きされていた。今回 **Anthropic Claude API + Vercel** に決定。リポジトリ構成は「CLAUDE.mdの本リポジトリはiOSアプリ専用」という原則と矛盾するが、1人開発でリポジトリ管理を一元化したいという判断で、このリポジトリ内の`api/`をVercelのサーバールートにした（CLAUDE.md自体の更新は今回は行っていない）。
- `vercel dev`は`.env.local`を直接読まず、`vercel env add`でVercel側の"development"環境変数として登録する必要があると分かった。`.env.local`を手で編集しても`process.env`に反映されず、ローカル動作確認で時間を使った。
- Vercelの`api/`配下は既定で誰でも呼べる公開エンドポイントになる。LLMのAPIキー自体はサーバー側に隠せても、エンドポイントの悪用（コスト消費）は別問題として残るため、`x-app-secret`ヘッダーによる簡易な共有シークレット認証を追加する設計にした。

### 次回やること

- 4.3.3: `RNSVGSvgView`エラーの解決確認（解決したらPR作成）
- 5.1.3: 実際のAPIキーをVercelに設定し、デプロイ・生成結果の禁止表現チェック（利用者作業）
- PR #5のマージ判断（利用者）

### 未解決・保留

- `docs/architecture.md`が空ファイルだった（CLAUDE.mdでは作成済み扱いだが実体がない）。今回は対応せず、気づいた点として記録のみ。

---

## 2026-08-10

### 睡眠データの構造確認とソース重複の解決

睡眠データの構造を確認して、複数ソース混在の問題を発見。Apple Watch と AutoSleep が同じ夜を重複記録していた。歩数の重複カウントと同じ構造で、HealthKit を扱ううえで繰り返し出てくる落とし穴だと分かった。

対策として Apple Watch にソースを限定し、Watch がない場合はサンプル数最多のソース1つに絞るフォールバックを実装。ヘルスケアアプリの表示と一致することを確認(8時間44分)。

readonly 配列の型エラーで少し詰まった。ライブラリが `readonly T[]` を返すため、受け取る関数の引数も readonly にする必要があった。
